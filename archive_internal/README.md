# archive_internal

Local-only workspace for AI assistants, plans, and scratch notes.

Everything in this directory is **gitignored** except this README. Do not put
secrets here — use your OS keychain or `.env.local` (also gitignored).

Canonical project documentation lives in `docs/` and `CONTRIBUTING.md`.

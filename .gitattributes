# Keep Markdown line endings stable as LF regardless of the editor/OS or
# tooling that touches them. ROADMAP.md in particular was flip-flopping between
# CRLF and LF across commits, producing whole-file diffs for one-line edits.
*.md text eol=lf

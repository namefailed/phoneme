//! Auto-generated recording titles: a cheap text heuristic, with an optional
//! LLM pass on top.

// Implementation lands with the auto-titles change; this stub only reserves
// the module so parallel work doesn't fight over lib.rs.

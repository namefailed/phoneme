// Stub — full implementation lands in Plan 5 Tasks 7 & 8.
export class FirstRunWizard {
  constructor(container: HTMLElement, _onClose: () => void) {
    container.innerHTML = `<div class="empty"><p>First-run wizard — coming soon</p></div>`;
  }

  dispose() {}
}

// Stub — full implementation lands in Plan 5 Tasks 4 & 5.
export class SettingsView {
  constructor(container: HTMLElement, _onClose: () => void) {
    container.innerHTML = `<div class="empty"><p>Settings — coming soon</p></div>`;
  }

  dispose() {}
}

// Stub — full implementation lands in Plan 5 Task 6.
export class DoctorView {
  constructor(container: HTMLElement, _onClose: () => void) {
    container.innerHTML = `<div class="empty"><p>Doctor — coming soon</p></div>`;
  }

  dispose() {}
}
